# Server Configuration
PORT=8000

# Session Configuration
# Generate a secure random string for production using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=your-secret-here-change-this-to-a-random-string-in-production

# Database Configuration
DATABASE_PATH=./var/db/bank.db
SESSION_DB_PATH=./var/db/sessions.db

# Logging Configuration
LOG_LEVEL=debug
LOG_DIR=./var/log
